class Solution {
    public List<List<Integer>> threeSum(int[] nums) {
        /*Arrays.sort(nums);
	// to store list of lists that can hold List<Integer> objects.
	List<List<Integer>> answer=new ArrayList<List<Integer>>();
	// to create single Arraylist 
	ArrayList<Integer> temp;
	int left, right, sum;
	for(int i=0;i<nums.length;i++) {
		left=i+1;
		right=nums.length-1;
		while(left<right) {
			sum=nums[i]+nums[left]+nums[right];
			if(sum==0) {
				temp=new ArrayList<Integer>();
				temp.add(nums[i]);
				temp.add(nums[left]);
				temp.add(nums[right]);
				if(answer.contains(temp)==false) {
					answer.add(temp);
					left++;
					continue;
				}
				left++;
				continue;
			}
			else if(sum<0) {
				left++;
				continue;
			}
			else {
				right--;
				continue;
			}
		}
	}
	return answer;*/ 
//this above code has increased T.C

    Arrays.sort(nums); // Sort the input array
        List<List<Integer>> answer = new ArrayList<List<Integer>>(); // To store results

        int left, right, sum;
        for (int i = 0; i < nums.length - 2; i++) {
            // Skip duplicate numbers for the first number in the triplet
            if (i > 0 && nums[i] == nums[i - 1]) {
                continue;
            }

            left = i + 1;
            right = nums.length - 1;

            while (left < right) {
                sum = nums[i] + nums[left] + nums[right];
                if (sum == 0) {
                    // Add triplet to the result
                    answer.add(Arrays.asList(nums[i], nums[left], nums[right]));

                    // Move pointers and skip duplicates
                    left++;
                    right--;
                    while (left < right && nums[left] == nums[left - 1]) left++;
                    while (left < right && nums[right] == nums[right + 1]) right--;
                } else if (sum < 0) {
                    left++; // Move left pointer to increase sum
                } else {
                    right--; // Move right pointer to decrease sum
                }
            }
        }
        return answer;
    }
}
